#define _CRT_SECURE_NO_WARNINGS
#include "Phonebook.h"

#include <iostream>
#include <stdio.h>

int main()
{
	/*char* name = new char[5];
	printf("Input name of 4 symbols\n");
	for (int i = 0; i < 4; ++i)
	{
		scanf("%c", &name[i]);
	}
	name[4] = '\0';
	char* number = new char[5];
	printf("Input number of 4 symbols\n");
	for (int i = 0; i < 4; ++i)
	{
		scanf("%c", &number[i]);
	}
	number[4] = '\0';
	Subscriber a(name, number);
	a.printName();
	system("pause");
	a.printNumber();
	Subscriber b(name, number);
	Phonebook p;
	p.addNote(a);
	p.addNote(b);
	p.printAllNotes();*/

	char const * arr = "lala";
	printf("%s\n", arr);
	



	
	system("pause");
	return 0;
}